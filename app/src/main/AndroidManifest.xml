<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manu.dungeonmasterlibrary">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoToolbar">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/NoToolbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PersonajesActivity"
            android:label="@string/title_activity_personajes"
            android:theme="@style/NoToolbar" />
        <activity
            android:name=".UnoCreacionPersonajesActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DosCreacionPersonajesActivity"
            android:theme="@style/AppTheme" />
        -*º
        <activity android:name=".TresCreacionPersonajesActivity" />
        <activity android:name=".MostrarUnoPersonajeActivity" />
        <activity android:name=".TestActivity"></activity>
    </application>

</manifest>